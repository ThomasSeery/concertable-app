export interface Venue {
    id: number;
    listingId: number;
    name: string;
    about: string;
    longitude: string;
    latitude: string;
    imageUrl: string;
    county: string;
    town: string
    approved: boolean;
}